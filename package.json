{
  "name": "contract-generator-ai",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-prebuild": "cd backend && npm install",
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "deploy:aws": "cd backend && npx serverless deploy"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
